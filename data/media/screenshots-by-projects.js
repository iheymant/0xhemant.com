import { voicekeepScreenshots } from "../builds/voicekeep/screenshots.js";
import { tracekeepScreenshots } from "../builds/tracekeep/screenshots.js";

export const screenshotsByProject = {
  voicekeep: voicekeepScreenshots,
  tracekeep: tracekeepScreenshots,
};
